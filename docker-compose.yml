services:
  redis:
    image: redis:6.2.6-alpine
    expose:
      - "6379"
      - "16379"
    volumes:
      - "/home/harsh/TinyURL/redis.conf:/usr/local/etc/redis/redis.conf"
    command: ["redis-server", "/usr/local/etc/redis/redis.conf"]
